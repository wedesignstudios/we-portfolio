import React, { Component } from 'react';
import ReactDOM from 'react-dom';

class FormAddress extends Component {
  render() {
    return(
      <div>
        FormAddress goes here. Use this form to create new address if not exists.
      </div>
    );
  }
}

module.exports = FormAddress;